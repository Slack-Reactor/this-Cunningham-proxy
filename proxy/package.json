{
  "name": "proxy",
  "version": "1.0.0",
  "description": "proxy",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx nodemon server.js"
  },
  "proxy": {
    "/api/showcase": {
      "target": "http://localhost:3001/api/showcase",
      "changeOrigin": true
    }
  },
  "author": "chris cunningham",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
