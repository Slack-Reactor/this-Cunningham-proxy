{
  "name": "proxy",
  "version": "1.0.0",
  "description": "proxy",
  "main": "server.js",
  "scripts": {
    "start": "ttab npx nodemon server.js && ttab 'cd ../this-Cunningham-service; npm run start' && ttab 'cd ../sabateklu-service; npm run server' && ttab 'cd ../thomas-mcnutt-service; npm run start' && ttab 'cd ../collin-newman-service; npm run start'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "launch": ""
  },
  "author": "chris cunningham",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
